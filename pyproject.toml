[project]
name = "mavenir-assignment"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "unstructured[docx]>=0.25.2",
]

[project.scripts]
mavenir-assignment = "mavenir_assignment:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
